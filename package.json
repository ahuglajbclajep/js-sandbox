{
  "name": "js-sandbox",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "jest",
    "deploy": "gh-pages -d browser"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "jest": "^24.5.0"
  }
}
