{
  "name": "js-sandbox",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "python3 -m http.server -d browser",
    "deploy": "gh-pages -d browser"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
